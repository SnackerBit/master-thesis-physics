\documentclass[crop, tikz]{standalone}
\usepackage{mathtools}
\input{../../style.tex}
\begin{document}
	\def \openLegLength{30pt}
	\begin{tikzpicture}
		\node[tensorOrthoCenter, fill=generalTensorColor, inner sep=0] (W) at (0, \defaultDistanceNormal) {$W$};
		\node[tensorOrthoCenter, fill=generalTensorColor, inner sep=0] (W*) at (0, 0) {$W^*$};
		\draw[virtualLeg] (W) -- ++(\openLegLength, 0);
		\draw[virtualLeg] (W*) -- ++(\openLegLength, 0);
		\draw[virtualLeg] (-\openLegLength, \defaultDistanceNormal) -- (W);
		\draw[virtualLeg] (-\openLegLength, 0) -- (W*);
		\draw[virtualLegWithoutArrows] (W) -- (\openLegLength, \defaultDistanceNormal) to[out=0, in=90] (\openLegLength+\identityLegDistance, \defaultDistanceNormal/2) to[out=-90, in=0] (\openLegLength, 0) -- (W*);
		
		\node[] () at (\openLegLength+\defaultDistanceEquations/2+\identityLegDistance, \defaultDistanceNormal/2) {$=$};
		
		\draw[virtualLegWithoutArrows] (\defaultDistanceEquations+\openLegLength+\defaultTensorWidth/2, \defaultDistanceNormal) to[out=0, in=90] ++(+\identityLegDistance, -\defaultDistanceNormal/2) to[out=-90, in=0] ++(-\identityLegDistance, -\defaultDistanceNormal/2);
		\node[tensorOrthoCenter, fill=identityColor, inner sep=0] (eye) at (\defaultDistanceEquations+\openLegLength+\defaultTensorWidth/2+\identityLegDistance, \defaultDistanceNormal/2) {$\mathbb{P}$};
	\end{tikzpicture}
\end{document}